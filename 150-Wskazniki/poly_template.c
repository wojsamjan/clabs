// oblicz pierwiastek równania pierwszego stopnia:
//   b = a * x
//   x = b / a

#include <stdio.h>

void GetCoefficients( ? );
void Solve( ? );
void DisplayResults( ? );

int main() {
  double a, b, x;

  GetCoefficients( ? );
  Solve( ? );
  DisplayResults( ? );
}
